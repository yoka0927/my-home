# 空白作品画廊网站

这是可公开分享的网站副本，不包含原作者的作品图片、编辑记录、Git 历史、GitHub 仓库地址或 Vercel 网站地址。网站公开端以例图画廊为第一窗口，后接价格与活动、联系方式；背景视频和 BGM 都是可选媒体，默认不配置 BGM。

## 页面

- `/`：例图画廊、价格与活动、联系方式三个可滑动场景。
- `/works`：完整例图画廊，公开端只显示已经上传的图片。
- `/editor`：通过“后台管理软件”启动后使用的本地可视化编辑器。

## 画廊

- 画廊大模块可在后台自由添加、删除和重命名。
- 每个大模块中的图片支持单张和批量上传，批量图片按上传顺序追加。
- 未上传的待上传槽只在后台显示，公开画廊自动隐藏空模块和空图片。

图片点击后可预览大图。联系方式内容可在后台修改；无 BGM 时所有页面都不显示音量控件。

联系方式支持在本地后台右侧添加多个 QQ 联系按钮。按钮名称和 QQ 号可编辑、删除，只有填写有效 QQ 号的按钮才会发布到公开页面。

## 本地运行

建议直接运行套件中 `后台管理软件/启动后台管理软件.cmd`。首次运行会安装依赖，之后自动打开编辑器。

发布中心中的 GitHub 和 Vercel 均为空白状态，每位使用者需要使用自己的账号完成首次授权。
